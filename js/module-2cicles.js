"use strict";
//while     work  while it's true
//let count = 0;

//while (count < 10) {
//console.log(`Count: ${count}`);
//count += 1;
//}

//Змінна clientCounter зберігає кількість зайнятих номерів на поточний момент.
//Змінна maxClients зберігає загальну кількість номерів у готелі.
// let clientCounter = 18;
// const maxClients = 25;

// while (clientCounter <= maxClients) {
//   console.log(clientCounter);
//   clientCounter += 1;
// }

//Функція calculateTotal(number) приймає ціле число (параметр number). Доповни код функції так, щоб вона повертала суму всіх цілих чисел від одиниці до цього числа включно. Наприклад, якщо number дорівнює 3, то сума - це 1 + 2 + 3, тобто 6.
// function calculateTotal(number) {
//   let sum = 0; // Ініціалізуємо змінну для зберігання суми
//   let i = 1; // Початкове значення лічильника
//   // Виконуємо цикл поки i не перевищить number
//   while (i <= number) {
//     sum += i; // Додаємо поточне значення i до суми
//     i++; // Збільшуємо значення лічильника
//   }
//   return sum; // Повертаємо підсумкове значення
// }

// function calculateTotal(number) {
//   let i = 0;
//   let number = 7;
//   for (i = 0; i <= number; i += 1) {}
//   return calculateTotal;
//   console.log(calculateTotal);
// }
